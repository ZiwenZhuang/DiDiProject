/* This headfile defines the basic method to write to pgm file
basd on given gray scale matrix.
    In the given matrix, you don't have to normalize it, just
give the greatest and the smallest value of the matrix.
*/
#include <fstream>
#include <string>

using namespace std;

#include "matrix.h"
